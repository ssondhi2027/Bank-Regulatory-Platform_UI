{
  "name": "brp-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lambda API serving the bank-regulatory-platform BigQuery marts",
  "scripts": {
    "build": "node build.mjs",
    "package": "npm run build && cd dist && zip -qr ../lambda.zip . && cd ..",
    "dev": "node --watch src/local.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@google-cloud/bigquery": "^7.9.1",
    "google-auth-library": "^9.15.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
