# Lambda Function URL, no trailing slash.
VITE_API_BASE=https://xxxxxxxxxxxxxxxxxxxxxxxxxxxxx.lambda-url.ca-central-1.on.aws

# Must match the API_KEY set on the Lambda (terraform.tfvars' api_key). Ships
# in the public build, so it only filters naive bots — not real access control.
VITE_API_KEY=
