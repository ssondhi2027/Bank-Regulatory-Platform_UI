# Cloud Run service URL, no trailing slash.
VITE_API_BASE=https://brp-api-xxxxxxxxxxx.northamerica-northeast1.run.app

# Must match the API_KEY set on the Cloud Run service (terraform.tfvars'
# api_key). Ships in the public build, so it only filters naive bots — not
# real access control.
VITE_API_KEY=
